{
  "name": "password-sharing-tool",
  "version": "1.0.0",
  "description": "A tool to share passwords safely",
  "main": "src/server.js",
  "scripts": {
    "start": "node .",
    "start:dev": "npx nodemon",
    "migrate:up": "npx sequelize-cli db:migrate --",
    "migrate:down": "npx sequelize-cli db:migrate:undo --",
    "migrate:down:all": "npx sequelize-cli db:migrate:undo:all --",
    "migrate:options": "npx sequelize-cli db:migrate --help",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wilber Claudio",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-jwt": "^8.4.1",
    "install": "^0.13.0",
    "jsonwebtoken": "^9.0.0",
    "npm": "^9.7.1",
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "axios": "^1.4.0",
    "nodemon": "^2.0.22",
    "sequelize-cli": "^6.6.1"
  },
  "engines": {
    "node": ">=14",
    "npm": ">=9"
  }
}
